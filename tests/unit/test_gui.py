"""
GUI tests -- REMOVED

The GUI (YouTubeUpdaterGUI / PyQt6 desktop window) was removed from this project.
The application is now CLI-only (youtube_updater/cli.py).

These tests are preserved as a placeholder to document that the GUI
was intentionally removed and is no longer tested.
"""

import pytest


@pytest.mark.skip(reason="GUI has been removed from this project. App is now CLI-only.")
def test_gui_removed():
    """Placeholder documenting that the GUI was removed."""
    pass
